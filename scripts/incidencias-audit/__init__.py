"""Incidencias audit pipeline (SPEC-IA01).

Each numbered file (01_*..12_*) is a stage of the deterministic pipeline.
The orchestrator is `run.py`. Helpers prefixed with `_` are reusable modules.
"""
__version__ = "1.0.0"
